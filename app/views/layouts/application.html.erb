<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= favicon_link_tag 'img/albero.png' %>
  <%= stylesheet_link_tag 'application', media: 'all' %>
</head>
<body>
  <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
  
  <header class="top-nav">
    <div class="nav-left">
      <h1><%= yield :title || 'Basso Legnami logistics' %></h1>
    </div>
  </header>

  <div class="sidebar" id="sidebar">
    <div class="logo">
      <button class="toggle-button" onclick="toggleSidebar()">☰</button>
      <%= link_to image_tag('img/logo.png', alt: 'Logo'), root_path %>
    </div>
  </div>

  <main>
    <%= yield %>
  </main>

  <script>
    function toggleSidebar() { /* JavaScript originale */ }
    function showSubmenu(menuId) { /* JavaScript originale */ }
    document.addEventListener("DOMContentLoaded", function() { /* JavaScript originale */ });
  </script>
</body>
</html>
